import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Best AI Tools Directory & Latest AI Tools List - MagicBox AI",
  description: "Discover the top AI tools of 2024 with the MagicBox AI Directory! Explore the latest innovations in artificial intelligence technology.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
